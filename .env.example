# Required — from @BotFather on Telegram
BOT_TOKEN=your_bot_token_here

# Optional — sticker file_id sent before each price report (leave empty to skip)
# STICKER_FILE_ID=

# Optional — HTTP timeout per exchange API call in seconds (5–120, default 14)
# REQUEST_TIMEOUT_S=25
